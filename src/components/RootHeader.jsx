
// className={`text-blue-700 ${({ isActive }) => isActive ? text-orange-700 : text-blue-700}`}

export default function RootHeader() {
  return (
    <div className='flex bg-slate-600'>rootHeader</div>
  )
}